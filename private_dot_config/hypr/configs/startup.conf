# Startup
exec-once = waybar

# notifs
exec-once = swaync

exec-once = swww-daemon

exec-once = copyq

exec-once = rog-control-center

# wallpaper utils
exec-once = wallutils & waypaper --restore & wal -R
